interface:
  display_name: "Manga Loader"
  short_description: "搜索漫画、订阅更新、生成单章/合订/分册 EPUB，并校验输出质量"
  default_prompt: "使用 manga-loader 处理这个漫画任务。先遵守 SKILL.md；新环境先 bootstrap 和 doctor；如果用户只给标题先 search；最终结果优先交付 library/ 下的文件，不要把 runs/ 当成最终阅读目录。"
